# Cloudflare Worker exit

Deploy and docs: **[docs/cloudflare-setup.md](../../../docs/cloudflare-setup.md)**

```bash
cd relay/deploy/cloudflare && wrangler login && wrangler deploy
```

Edit `WORKER_HOST` in `worker.js` and `name` in `wrangler.toml` first.
